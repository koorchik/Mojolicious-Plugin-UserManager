<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    
    <style type="text/css">
        .green_msg {
            color:green;
            font-weight:bold;
        }
        .red_msg {
            color:red;
            font-weight:bold;
        }
    </style>
    
</head>
<body>
    <h1>Registration</h1>

    <%= form_for user_create => (method => 'post') => begin %>
    
    <span class='red_msg' ><%= flash 'error' %></span>
    <span class='green_msg' ><%= flash 'notice'%></span>
    
    <div class='fieldset' style='width: 400px'>
        <table class='form'>
            <% for my $field ( @{ all_fields_schema() } ) { %>
                <tr>
                    <td class='fieldname'><%= $field->{label} %></td>
                    <td><input type='text' name='<%= $field->{name} %>' value="<%= flash $field->{name} %>" /></td>
                </tr>
            <% } %>
            
            <tr>
                <td colspan="2"><%= recaptcha_html() if $captcha %></td>
            </tr>
            
            <tr>
                <td></td>
                <td style='padding-top: 7px;'><input type='submit' value='Register' /></td>
            </tr>
            
            <tr>
                <td></td>
                <td><a href='<%= url_for "auth_create_form" %>'> Back to Login </a></td>
            </tr>

        </table>

    </div>
    <% end %>

</body>
</html>